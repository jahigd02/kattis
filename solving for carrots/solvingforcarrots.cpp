#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;
typedef long long ll;

int main()
{
    int n, p; cin >> n >> p;
    for (int i = 0; i < n; i++)
    {
        string s; cin >> s;
    }

    cout << p;
}